/**
 * Legacy student web app URL — same as `SwitchToOldLMS.tsx` in
 * `experience-ui/apps/student-experience` (hardcoded; profile “Switch to OLD LMS” uses this, not env).
 */
export const LEGACY_STUDENT_LMS_URL = 'https://students-old.masaischool.com/' as const
