/** Minutes before scheduled start when join UI becomes visible (legacy LMS). */
export const JOIN_UI_VISIBLE_MINUTES_BEFORE_START = 10

/** Minutes before scheduled start when join button becomes active (legacy LMS). */
export const JOIN_UI_ACTIVE_MINUTES_BEFORE_START = 5

/** Minutes before scheduled start when zoom link is withheld (legacy API). */
export const ZOOM_LINK_VISIBLE_MINUTES_BEFORE_START = 10
