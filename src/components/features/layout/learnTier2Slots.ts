/**
 * Stable DOM ids for the desktop navbar's Learn Tier 2 portal targets
 * (`AppNavbar.tsx`). The Learn page portals its own tab switcher and program
 * switcher into these so their state/data stays owned by the page (URL search
 * params, `LearnLayout`'s query) instead of being duplicated in the navbar.
 */
export const LEARN_TIER2_TABS_SLOT_ID = 'learn-tier2-tabs-slot'
export const LEARN_TIER2_PROGRAM_SLOT_ID = 'learn-tier2-program-slot'
