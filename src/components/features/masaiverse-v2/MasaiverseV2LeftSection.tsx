/**
 * Masaiverse v2 — left section.
 *
 * Placeholder / empty canvas. Owns the divider border that separates it from
 * the right section (no margin/gap between the two).
 */
export default function MasaiverseV2LeftSection() {
  return (
    <aside className="w-[20%] shrink-0 border-r border-[#E5E7EB] p-6">left</aside>
  )
}
